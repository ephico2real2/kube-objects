# Relock record — 2026-08-25: cgi, json-jwt, mail

Closing the deferred scanner backlog (Quay/Clair and trivy agreed on the set):
GHSA-mhwm-jh88-3gjf + GHSA-gh9q-2xrm-x6qv (cgi 0.3.6, fixed 0.3.7),
GHSA-c8v6-786g-vjx6 (json-jwt 1.15.3, fixed 1.15.3.1), and trivy's mail 2.7.1 finding
(fixed 2.9.1). The remaining `json 2.9.1` (Low) lives in the BASE image's bundled default
gems — only a Red Hat base rebuild closes it; tracked in trivy-local-scan/ history.

## The exact commands (the evidence is reproducible, not asserted)

```sh
# one gem at a time, resolved against the runtime Ruby inside a container:
ci_scripts/relock.sh cgi json-jwt mail

# verify the targeted bumps:
git diff -- docker/Gemfile.lock | grep -E "^[+-]    (cgi|json-jwt|mail) "

# verify the FULL gem-level movement (nothing outside the requested subtrees):
git diff -- docker/Gemfile.lock | grep -E "^[+-]    [a-z]" | sort | uniq -c | sort -rn
```

## Measured result (identical across all four image repos)

Targets: `cgi 0.3.6 -> 0.3.7`, `json-jwt 1.15.3 -> 1.15.3.1`, `mail 2.7.1 -> 2.9.1`.

Transitive movement, all within the requested gems' own dependency subtrees:
- mail 2.9 splits its net libraries out: + net-imap 0.6.6, net-pop 0.1.2, net-smtp 0.5.1,
  net-protocol 0.2.2, timeout 0.6.1
- json-jwt's dependencies: bindata 2.4.15 -> 3.0.0 (major — exercised by the boot battery's
  `require "json/jwt"`), httpclient 2.8.3 -> 2.9.0, connection_pool 2.4.1 -> 2.5.5,
  + mutex_m 0.3.0

Published as the next hardening pass of each repo (a gem change is a new pass; prior passes
stay frozen as published).
